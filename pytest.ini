[pytest]
# asyncio_mode=auto is what makes the existing @pytest.mark.asyncio tests run at
# all; without it every async test errored or was silently skipped, so the
# "tests exist" claim was not operative.
asyncio_mode = auto
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
